#!/bin/sh

id=`id | cut -f1 -d'('`
if [ $id != "uid=0" ]
then
	echo
	echo "You must be root to run this script"
	echo
	exit
fi

pkg_add git

cd /home
git clone https://github.com/iudx/iudx-sub-ca

cd iudx-sub-ca
./setup.openbsd
