CREATE TABLE IF NOT EXISTS employee(
                            email_id VARCHAR NOT NULL primary key,
                            first_name TEXT NOT NULL,
                            last_name  TEXT NOT NULL,
                            title TEXT NOT NULL,
                            certificate_class VARCHAR NOT NULL
		     );
